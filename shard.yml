name: cli_tester
version: 0.1.0

authors:
  - Dominic Sisneros <dsisnero@gmail.com>

description: |
  A Crystal shard for testing Command Line Interface (CLI) applications
  by executing them as a user would, inspired by cli-testing-library.

crystal: ">= 1.11.0" # Using Dir.mktmpdir requires >= 1.1.0, let's use a more recent baseline

dependencies:
  yaml:
    github: crystal-lang/yaml
    version: ~> 0.2.0 # Added for Shard parser

targets:
  cli_tester:
    main: src/cli_tester.cr

license: MIT
